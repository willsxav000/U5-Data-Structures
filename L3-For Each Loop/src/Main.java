import com.sun.scenario.effect.impl.sw.sse.SSEBlend_SRC_OUTPeer;
import java.util.Arrays;
import java.util.Scanner;
public class Main {


    public static void main(String[] args) {
        /*Scanner charles = new Scanner(System.in);
       /* String[] arr = new String[5];

        for(int i = 0; i < arr.length; i++){
            System.out.println("Enter a name");
            arr[i] = charles.nextLine();
        }

            for(String name: arr){
                System.out.println(name);
            }*/

        /*int[] daily = new int[10];
        double[] weekly = new double[2];

        for(int i = 0; i < weekly.length; i++){
            for(int j = 0; j < daily.length; j++){
                System.out.println("Please enter employee's hours: ");
                daily[j] = charles.nextInt();
            weekly[i] += daily[j]*9.50;
            }
        }

        for(double weeklyTotal: weekly){
            System.out.println(weeklyTotal);
        }*/

        MMMR calculate = new MMMR();

        double[] testArray = {2, 2, 3, 3, 4};

        System.out.println("The mean is: " + calculate.mean(testArray);
        System.out.println("The median is: " + calculate.median(testArray));
        System.out.println("The range is: " + calculate.range(testArray));
        System.out.println(("The mode is: " + calculate.mode(testArray));
    }










    }


    }

